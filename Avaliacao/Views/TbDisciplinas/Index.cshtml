@model IEnumerable<Avaliacao.Models.TbDisciplina>

@{
    ViewBag.Title = "Index";
}


<h2>Index</h2>


<form>
    <div class="row">
        <div class="col-md-6">
            <label class="text-left">Buscar</label>
            <input class="form-control" type="text" name="buscaPorNomeDisciplina" />
            <input class="btn btn-primary" type="submit" value="Buscar" />
        </div>
    </div>
</form>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_disciplina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nome_disciplina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cargahoraria_disciplina)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.id_disciplina)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.nome_disciplina)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.cargahoraria_disciplina)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.id_disciplina }) |
        @Html.ActionLink("Details", "Details", new { id = item.id_disciplina }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.id_disciplina })
    </td>
</tr>
}

</table>
